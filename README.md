# PsiNote

Plataforma profissional para psicólogos.

## Tecnologias
- React + Vite
- Firebase (Auth, Firestore)
- Supabase Storage
- GitHub Pages

## Como rodar localmente
1. Clone o repositório
2. Crie um arquivo `.env` com as variáveis (veja `.env.example`)
3. `npm install`
4. `npm run dev`
